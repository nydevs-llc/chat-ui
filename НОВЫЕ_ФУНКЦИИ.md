# Новые функции библиотеки чата

## 🎯 Реализованные возможности

### 1. Скролл к определенному сообщению

**Описание:** Возможность программно прокручивать чат к конкретному сообщению по его ID.

**Использование:**
```swift
ChatView(messages: messages, /* ... */)
    .scrollToMessage(messageId: "message-123", animated: true)
```

**Применение:**
- Переход к непрочитанным сообщениям
- Навигация по упоминаниям (@mentions)
- Переход к ответам и цитированиям
- Deep linking в конкретное сообщение

---

### 2. Отслеживание прочитанности сообщений

**Описание:** Эффективная система отслеживания видимых сообщений с автоматической отправкой данных на бэкенд.

**Ключевые особенности:**
- ✅ Автоматическое отслеживание видимых сообщений
- ✅ Debouncing для оптимизации сетевых запросов
- ✅ Фильтрация по минимальному времени видимости
- ✅ Определение максимального прочитанного сообщения
- ✅ Игнорирование собственных сообщений

**Использование:**
```swift
ChatView(messages: messages, /* ... */)
    .enableMessageReadTracking(
        debounceInterval: 1.0,              // Интервал накопления (сек)
        minimumVisibilityDuration: 0.5,     // Минимальное время видимости (сек)
        onMessageRead: { messageId in
            // Отправка на бэкенд
            APIClient.markMessageAsRead(messageId: messageId)
        }
    )
```

---

## 📊 Алгоритм отслеживания прочитанности

### Принцип работы:

1. **Отслеживание появления сообщений**
   - При появлении сообщения в зоне видимости запоминается его ID и время появления
   - Только сообщения от других пользователей (`!isCurrentUser`)

2. **Отслеживание исчезновения**
   - При выходе из зоны видимости сообщение удаляется из списка отслеживаемых

3. **Периодическая обработка**
   - Каждые `debounceInterval` секунд система проверяет видимые сообщения
   - Отфильтровываются только те, что видимы >= `minimumVisibilityDuration`

4. **Определение максимального**
   - Из отфильтрованных выбирается самое новое по `createdAt`
   - Если ID изменился - вызывается callback

5. **Отправка на бэкенд**
   - В callback приходит ID максимального прочитанного сообщения
   - Это означает, что все сообщения до него тоже прочитаны

### Преимущества подхода:

✅ **Эффективность**: Минимум сетевых запросов благодаря debouncing
✅ **Точность**: Учитывается реальное время просмотра сообщения
✅ **Производительность**: O(n) памяти, где n - количество видимых сообщений (5-15)
✅ **Простота**: Один callback вместо сложной логики

---

## 🔧 Технические детали

### Структура файлов:

```
Sources/ExyteChat/
├── ChatView/
│   ├── ChatView.swift              # Добавлен API для новых функций
│   ├── SonataUIList.swift          # Интеграция с UICollectionView
│   └── MessageReadTracker.swift    # Новый: Менеджер отслеживания
```

### Добавленные методы:

**ChatView.swift:**
- `scrollToMessage(messageId:animated:)` - Скролл к сообщению
- `enableMessageReadTracking(...)` - Включение отслеживания

**SonataUIList.swift:**
- Добавлено уведомление `.onScrollToMessage`
- Метод `scrollToMessage(messageId:animated:)` в Coordinator
- Интеграция с `MessageReadTracker` в `willDisplay` / `didEndDisplaying`

**MessageReadTracker.swift (новый файл):**
- Класс для управления отслеживанием прочитанности
- Debouncing через Timer
- Фильтрация по времени видимости
- Callback механизм

---

## 📱 Примеры использования

### Базовый пример

```swift
struct ConversationView: View {
    @State private var messages: [Message] = []

    var body: some View {
        ChatView(
            messages: messages,
            chatType: .conversation,
            didSendMessage: sendMessage
        )
        .enableMessageReadTracking { messageId in
            // Простая отправка на бэкенд
            Task {
                try? await APIClient.markAsRead(messageId)
            }
        }
    }
}
```

### С обработкой ошибок и retry

```swift
.enableMessageReadTracking { messageId in
    Task {
        var attempt = 0
        while attempt < 3 {
            do {
                try await APIClient.markAsRead(messageId)
                break
            } catch {
                attempt += 1
                try? await Task.sleep(nanoseconds: UInt64(pow(2.0, Double(attempt)) * 1_000_000_000))
            }
        }
    }
}
```

### Скролл к первому непрочитанному

```swift
.onAppear {
    if let firstUnreadId = viewModel.firstUnreadMessageId {
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
            NotificationCenter.default.post(
                name: .onScrollToMessage,
                object: firstUnreadId
            )
        }
    }
}
```

---

## 🎛 Настройка параметров

### Рекомендованные значения:

| Сценарий | debounceInterval | minimumVisibilityDuration |
|----------|------------------|---------------------------|
| Обычный чат | 1.0 сек | 0.5 сек |
| Активный групповой чат | 1.5-2.0 сек | 0.7 сек |
| Real-time индикаторы | 0.3-0.5 сек | 0.2 сек |
| Строгий контроль | 1.0 сек | 1.0-2.0 сек |

---

## 🚀 Production рекомендации

### 1. Batch-обработка на бэкенде

Вместо отправки каждого ID отдельно, накапливайте их:

```swift
class ReadReceiptManager {
    private var pending: Set<String> = []

    func track(_ messageId: String) {
        pending.insert(messageId)
        scheduleBatchSend()
    }

    private func scheduleBatchSend() {
        // Отправка batch через 5 секунд
    }
}
```

### 2. Локальное кэширование

```swift
// Сохранить локально для offline
UserDefaults.standard.set(messageId, forKey: "lastRead_\(chatId)")

// Sync с бэкендом асинхронно
Task.detached {
    try? await APIClient.sync()
}
```

### 3. Интеграция с Firestore

```swift
.enableMessageReadTracking { messageId in
    Task {
        try? await Firestore.firestore()
            .collection("chats/\(chatId)/participants")
            .document(userId)
            .setData([
                "lastReadMessageId": messageId,
                "lastReadAt": FieldValue.serverTimestamp()
            ], merge: true)
    }
}
```

---

## ✅ Что было реализовано

1. ✅ Скролл к определенному сообщению через NotificationCenter
2. ✅ Публичный API `scrollToMessage(messageId:animated:)`
3. ✅ Класс `MessageReadTracker` с debouncing
4. ✅ Интеграция в `SonataUIList` через `willDisplay`/`didEndDisplaying`
5. ✅ Фильтрация по минимальному времени видимости
6. ✅ Автоматическое игнорирование собственных сообщений
7. ✅ Callback механизм для отправки на бэкенд
8. ✅ Публичный API `enableMessageReadTracking(...)`
9. ✅ Полная документация с примерами

---

## 📖 Дополнительная документация

Подробная документация с расширенными примерами доступна в файле `FEATURE_DOCUMENTATION.md`
